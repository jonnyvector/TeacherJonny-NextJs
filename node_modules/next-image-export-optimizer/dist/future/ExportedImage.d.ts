/// <reference types="react" />
import { ImageProps, StaticImageData } from "next/future/image";
export interface ExportedImageProps extends Omit<ImageProps, "src" | "loader" | "quality"> {
    src: string | StaticImageData;
    useWebp?: boolean;
}
declare function ExportedImage({ src, priority, loading, className, width, height, useWebp, onLoadingComplete, unoptimized, placeholder, blurDataURL, style, onError, ...rest }: ExportedImageProps): JSX.Element;
export default ExportedImage;
